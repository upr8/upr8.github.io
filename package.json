{
  "name": "personal-web-site",
  "version": "2.0.0",
  "private": false,
  "description": "A personal website built with Astro.",
  "author": "Saeed Asaiyan",
  "keywords": [
    "astro",
    "blog",
    "personal-website"
  ],
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "gh-pages-action": "gh-pages -d dist -b release --remove '**/*'",
    "deploy-action": "npm run build && npm run gh-pages-action",
    "deploy": "npm run build && gh-pages -d dist -b release --remove '**/*'",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "astro check && tsc --noEmit",
    "biome:check": "biome check .",
    "biome:check:apply": "biome check . --write",
    "biome:check:apply:unsafe": "biome check . --apply-unsafe",
    "biome:format": "biome format . --write",
    "fix": "npm run biome:format && npm run biome:check:apply",
    "fix:unsafe": "npm run biome:format && npm run typecheck && npm run biome:check:apply:unsafe"
  },
  "license": "MIT",
  "dependencies": {
    "@astrojs/mdx": "^4.3.13",
    "@astrojs/rss": "^4.0.14",
    "@astrojs/sitemap": "^3.6.1",
    "@fontsource-variable/ibm-plex-sans": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/ibm-plex-serif": "^5.2.7",
    "@shikijs/transformers": "^3.21.0",
    "astro": "^5.16.8",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-external-links": "^3.0.0",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@tailwindcss/postcss": "^4.1.18",
    "@types/node": "^24.10.4",
    "gh-pages": "^6.3.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3"
  }
}
